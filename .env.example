# Local MongoDB connection (default)
MONGODB_URI=mongodb://127.0.0.1:27017/nudge

# MongoDB Atlas Cloud (Uncomment and replace with your cluster URI):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.abcde.mongodb.net/nudge?retryWrites=true&w=majority

PORT=3000
